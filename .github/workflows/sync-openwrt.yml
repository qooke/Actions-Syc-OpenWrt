name: Sync OpenWrt to owrt repo

on:
  schedule:
    - cron: "0 2 1 1,6 *"
  workflow_dispatch:

concurrency:
  group: sync-openwrt
  cancel-in-progress: true

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout owrt repository
        uses: actions/checkout@v4
        with:
          repository: qooke/owrt
          token: ${{ secrets.OWRT_PAT }}
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "qooke"
          git config user.email "83923036@qq.com"

      - name: Add upstream and fetch
        run: |
          git remote add upstream https://github.com/openwrt/openwrt.git || true
          git fetch --prune --tags upstream

      - name: Update main from upstream
        run: |
          git checkout main
          git rebase upstream/main

      - name: Rebase dev onto updated main
        run: |
          git checkout dev
          git rebase main

      - name: Rebase tmp onto updated main
        run: |
          git checkout tmp
          git rebase main

      - name: Push branches
        run: |
          git push origin main --force-with-lease
          git push origin dev --force-with-lease
          git push origin tmp --force-with-lease
